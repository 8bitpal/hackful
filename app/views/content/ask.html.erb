<% content_for :head do %>
	<%= auto_discovery_link_tag :rss, "/ask.rss" %>
<% end %>

<% @posts.each do |post| %>
	<%= build_post_preview(post) %>
<% end %>

<span class="page_navigation"><%= link_to("< previous", "/ask?page="+(@page.to_i-1).to_s) if @page > 0 %> &nbsp;&nbsp; <%= link_to("next >", "/ask?page="+(@page.to_i+1).to_s) if @posts.count > 19 %></span>
